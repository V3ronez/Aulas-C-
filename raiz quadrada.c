#include<stdio.h>
#include<math.h>

int main(void){
    float x,y;

    printf("Digite o valor de x: ");
    scanf("%d", &x);

    y = sqrt(x); // sqrt é a função que calcula raiz.

    printf("A raiz  quadrada de )
}
